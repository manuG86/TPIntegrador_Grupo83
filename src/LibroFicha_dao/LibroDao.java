package LibroFicha_dao;

import LibroFicha_entities.Libro;

public interface LibroDao extends Dao<Libro> {
    // Métodos específicos si querés, e.g. buscar por título
    // Libro findByTitulo(String titulo) throws Exception;
}
